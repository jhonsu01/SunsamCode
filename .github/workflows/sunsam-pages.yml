# Sunsam Code: publica la página del proyecto (docs/) en GitHub Pages.
# Requisito único: Settings → Pages → Build and deployment → Source: "GitHub Actions".
name: Sunsam pages

on:
  push:
    branches: [main]
    paths: ["docs/**", ".github/workflows/sunsam-pages.yml"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: sunsam-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs
      - id: deployment
        uses: actions/deploy-pages@v4
